<hr>
<div align="center">
 [**📜 Índice**](../../README.md)
</div>
<hr>

# Diseño Físico
## 𝄜 Tabla `CLIENTE`


|Año|	Nuevos Clientes|	Total Clientes|	Crecimiento|
|--------|---------------|--------|---------------|
|1	|10000|	10000	|---|
|2	|10700|	20700	|7%|
|3	|11556|	32256	|8%|

```ts
CREATE TABLE MODULO_CLIENTES.CLIENTE
(
  cod_cliente UUID NOT NULL DEFAULT uuid_generate_v4(),
  fecha_registro_cliente TIMESTAMP NOT NULL DEFAULT NOW(),
  ultima_actividad_cliente TIMESTAMP NOT NULL DEFAULT NOW(),
  cod_persona UUID NOT NULL,
  PRIMARY KEY (cod_cliente),
  FOREIGN KEY (cod_persona) REFERENCES MODULO_CLIENTES.PERSONA(cod_persona),
  UNIQUE (cod_persona)
);
```
<br>

|Atributo|Formato|Tamaño|
|--------|-------|------|
|cod_cliente|**UUID**|16 bytes|
|fecha_registro_cliente|**TIMESTAMP**|8 bytes|
|ultima_actividad_cliente|**TIMESTAMP**|8 bytes|
|cod_persona|**UUID**|16 bytes|

**Total: 48 bytes x registro**

### **Parámetros físicos (POSTGRES):**<br>

```ts
CALCULOS: 

Espacio Libre por Bloque = (8 192 − 24) ∗ 0.8 = 6 534.4 bytes por bloque

Cantidad de Filas por Bloque = (6 534.4)/(48 + 23) ≈ 92.03 ≈ 92 filas por bloque

Cantidad de Bloques ≈ (32 256)/(92) ≈ 350.61 ≈ 351 bloques

Tamaño Total ≈ 351 ∗ 8 192 ≈ 2 875 392 bytes ≈ 2.75 mb
```

|Atributo|Formato|
|--------|-------|
|Tamaño de bloque|8 KB|
|Tamaño de extent|---|
|Estructura del registro y tamaño del registro lógico| 71 bytes x fila <br> 92 filas x bloque|
|Espacio libre reservado|``` ALTER TABLE emp SET (FILLFACTOR = 80);```|
|Tamaño total estimado|2.75 mb|

## 𝄜 Tabla `CANJE`

|Año|	Numero de canjes|	Total canjes|	Crecimiento|
|--------|---------------|--------|---------------|
|1	|900|900|---|
|2	|1620|2520|8%|
|3	|1782|4302|10%|


```ts
CREATE TABLE MODULO_CLIENTES.CANJE
(
  cod_canje UUID NOT NULL DEFAULT uuid_generate_v4(),
  fecha_hora_canje TIMESTAMP NOT NULL DEFAULT NOW(),
  monto_canje NUMERIC(10, 2) NOT NULL,
  fecha_entrega TIMESTAMP,
  cod_usuario UUID NOT NULL,
  cod_estado_canje UUID NOT NULL,
  cod_maestro UUID NOT NULL,
  PRIMARY KEY (cod_canje),
  FOREIGN KEY (cod_usuario) REFERENCES MODULO_CLIENTES.USUARIO(cod_usuario),
  FOREIGN KEY (cod_estado_canje) REFERENCES MODULO_CLIENTES.ESTADO_CANJE(cod_estado_canje),
  FOREIGN KEY (cod_maestro) REFERENCES MODULO_CLIENTES.MAESTRO(cod_maestro),
  CONSTRAINT chk_monto_canje_positivo CHECK (monto_canje > 0),
  CONSTRAINT chk_fecha_entrega_valida CHECK (fecha_entrega IS NULL OR fecha_entrega >= fecha_hora_canje)
);
```
<br>

|Atributo|Formato|Tamaño|
|--------|-------|------|
|cod_canje|**UUID**|16 bytes|
|fecha_hora_canje|**TIMESTAMP**|8 bytes|
|monto_canje|**NUMERIC(10, 2)**|14 bytes|
|fecha_entrega|**TIMESTAMP**|8 bytes|
|cod_usuario|**UUID**|16 bytes|
|cod_estado_canje|**UUID**|16 bytes|
|cod_maestro|**UUID**|16 bytes|

**Total: 94 bytes x registro**

### **Parámetros físicos (POSTGRES):**<br>

```ts
CALCULOS: 

Espacio Libre por Bloque = (8 192 − 24) ∗ 0.8 = 6 534.4 bytes por bloque

Cantidad de Filas por Bloque = (6 534.4)/(94 + 23) ≈ 55.85 ≈ 55 filas por bloque

Cantidad de Bloques ≈ (4 302)/(55) ≈ 78.22 ≈ 79 bloques

Tamaño Total ≈ 79 ∗ 8 192 ≈ 647 168 bytes ≈ 0.62 mb
```

|Atributo|Formato|
|--------|-------|
|Tamaño de bloque|8 KB|
|Tamaño de extent|---|
|Estructura del registro y tamaño del registro lógico| 117 bytes x fila <br> 55 filas x bloque|
|Espacio libre reservado|``` ALTER TABLE emp SET (FILLFACTOR = 80);```|
|Tamaño total estimado|0.62 mb|


<hr>
<div align="center">
 [**📜 Índice**](../../README.md)
</div>
<hr>

